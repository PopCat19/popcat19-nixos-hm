# Context

- `formatter.nix` — Formatter configuration for flake-parts
- `hosts.nix` — Helper function to generate NixOS system configurations
- `modules.nix` — Export optional NixOS configuration modules
- `nixos.nix` — NixOS configuration module for flake-parts
- `overlays.nix` — Define architecture-aware package overlays
