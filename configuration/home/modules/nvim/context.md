# modules/home/nvim/context.md

- nvim.nix — entry point; enables nixvim and imports submodules
- options.nix — sets core vim options (indentation, line numbers, etc.)
- plugins.nix — declares and configures plugins
- keymaps.nix — defines all keybindings
- lsp.nix — configures LSP servers and completion
